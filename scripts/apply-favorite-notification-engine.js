const fs=require('fs');
// Legacy patch retained for history. The current favorite notification engine is
// applied by scripts/apply-home-artwork-smart-favorites.js.
// Keep this file valid JavaScript/TypeScript-check input so CI cannot fail on it.
console.log('Legacy favorite notification patch skipped: current engine is applied by apply-home-artwork-smart-favorites.js');
