const fs=require('fs');
function rep(text,search,replacement,label){const next=text.replace(search,replacement);if(next===text)throw new Error(`Patch not applied: ${label}`);return next}
let main=fs.readFileSync('src/MainApp.tsx','utf8');
let root=fs.readFileSync('src/Root.tsx','utf8');

main=rep(main,"import{supabase}from'./supabase';","import{supabase}from'./supabase';\nimport{Notifications}from'./notifications';",'notifications import');

main=rep(main,"  const relatedForSport=(sportId:string)=>favoriteCatalog.filter((x:any)=>x.sport===sportId&&x.group!=='Sports');\n  async function toggleFavorite(id:string){",`  const relatedForSport=(sportId:string)=>favoriteCatalog.filter((x:any)=>x.sport===sportId&&x.group!=='Sports');
  const normalizeFavoriteText=(value='')=>value.normalize('NFD').replace(/[\\u0300-\\u036f]/g,'').toLowerCase().replace(/[’']/g,"'");
  const sportFavoriteId=(p:Program)=>{const s=sportType(p);return s==='Football'?'football':s==='Tennis'?'tennis':s==='Rugby'?'rugby':s==='Basket'?'basket':s==='Handball'?'handball':s==='Cyclisme'?'cycling':s==='F1 / Auto'?'f1':s==='Moto'?'moto':s==='Golf'?'golf':s==='Natation'?'swimming':s==='Athlétisme'?'athletics':s==='Volley'?'volleyball':s==='Sports de combat'?'combat':''};
  const programMatchesFavorites=(p:Program)=>{const title=normalizeFavoriteText(p.title);return favorites.some(id=>{const item:any=favoriteCatalog.find((x:any)=>x.id===id);if(!item)return false;if(item.group==='Sports')return sportFavoriteId(p)===item.id;if(item.group==='Émissions')return title.includes(normalizeFavoriteText(item.label));if(item.group==='Clubs'||item.group==='Équipes'||item.group==='Compétitions'){if(item.sport&&sportFavoriteId(p)!==item.sport)return false;const label=normalizeFavoriteText(item.label);const aliases:Record<string,string[]>={psg:['psg','paris saint-germain','paris sg'],om:['marseille','olympique de marseille'],ol:['lyon','olympique lyonnais'],'champions-league':['ligue des champions','champions league'],ligue1:['ligue 1'],nba:['nba'],euroleague:['euroleague'],'tour-france':['tour de france'],motogp:['motogp'],'f1-championship':['formule 1','formula 1','f1']};return (aliases[item.id]||[label]).some(v=>title.includes(normalizeFavoriteText(v)))}return false})};
  async function toggleFavorite(id:string){`,'favorite matcher');

main=rep(main,"  useEffect(()=>{if(userId&&screen!=='auth')setScreen(initialScreen)},[initialScreen]);",`  useEffect(()=>{if(userId&&screen!=='auth')setScreen(initialScreen)},[initialScreen]);
  useEffect(()=>{if(!userId||reviewMode)return;let cancelled=false;(async()=>{try{const scheduled=await Notifications.getAllScheduledNotificationsAsync();for(const req of scheduled){if((req.content.data as any)?.type==='favorite_start')await Notifications.cancelScheduledNotificationAsync(req.identifier)}const{data:pref}=await supabase.from('notification_preferences').select('favorite_alerts').eq('user_id',userId).maybeSingle();if(cancelled||pref?.favorite_alerts===false||!favorites.length)return;const current=Date.now();for(const p of programs){const start=programStart(p,current);if(start<=current||start-current>24*60*60*1000||!programMatchesFavorites(p))continue;await Notifications.scheduleNotificationAsync({content:{title:'⭐ Ton favori commence',body:`${p.title} commence maintenant sur ${p.channel}.`,sound:'default',data:{type:'favorite_start',programId:p.id}},trigger:{type:Notifications.SchedulableTriggerInputTypes.DATE,date:new Date(start),channelId:'onlive'}})}}catch(e){console.warn('Favorite notification scheduling failed',e)}})();return()=>{cancelled=true}},[userId,reviewMode,programs,favorites]);`,'favorite schedule effect');

root=rep(root,"useEffect(()=>{const sub=Notifications.addNotificationResponseReceivedListener(response=>{const d:any=response.notification.request.content.data||{};if((d.type==='chat_message'||d.type==='chat_invite')&&d.roomId){setChatTarget(null);setNotificationRoomId(String(d.roomId));setMode('chat')}else if(d.type==='watching'){setNotificationRoomId(null);setChatTarget(null);setAppSection('home');setMode('app')}});return()=>sub.remove()},[]);","useEffect(()=>{const sub=Notifications.addNotificationResponseReceivedListener(response=>{const d:any=response.notification.request.content.data||{};if((d.type==='chat_message'||d.type==='chat_invite')&&d.roomId){setChatTarget(null);setNotificationRoomId(String(d.roomId));setMode('chat')}else if(d.type==='watching'||d.type==='favorite_start'){setNotificationRoomId(null);setChatTarget(null);setAppSection('home');setMode('app')}});return()=>sub.remove()},[]);",'notification navigation');

root=rep(root," const toggleNotificationPref=async(key:'chat_messages'|'chat_invites'|'favorite_alerts')=>{if(!userId)return;const next={...notificationPrefs,[key]:!notificationPrefs[key]};setNotificationPrefs(next);const{error}=await supabase.from('notification_preferences').upsert({user_id:userId,...next,updated_at:new Date().toISOString()},{onConflict:'user_id'});if(error){setNotificationPrefs(notificationPrefs);Alert.alert('Réglage impossible',error.message)}};",` const toggleNotificationPref=async(key:'chat_messages'|'chat_invites'|'favorite_alerts')=>{if(!userId)return;const next={...notificationPrefs,[key]:!notificationPrefs[key]};setNotificationPrefs(next);const{error}=await supabase.from('notification_preferences').upsert({user_id:userId,...next,updated_at:new Date().toISOString()},{onConflict:'user_id'});if(error){setNotificationPrefs(notificationPrefs);Alert.alert('Réglage impossible',error.message);return}if(key==='favorite_alerts'&&!next.favorite_alerts){const scheduled=await Notifications.getAllScheduledNotificationsAsync();for(const req of scheduled){if((req.content.data as any)?.type==='favorite_start')await Notifications.cancelScheduledNotificationAsync(req.identifier)}}};`,'cancel favorite schedules');

fs.writeFileSync('src/MainApp.tsx',main);fs.writeFileSync('src/Root.tsx',root);console.log('Favorite notification engine applied');
