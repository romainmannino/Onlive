name: Complete Onlive favorites and notifications (obsolete)

on:
  workflow_dispatch:

jobs:
  obsolete:
    runs-on: ubuntu-latest
    steps:
      - name: Obsolete workflow
        run: echo "This legacy workflow is disabled. Favorites changes are applied by the dedicated workflows."
